#include<stdio.h>
/**
 * main -Prints message to terminal using print f command
 *
 * Return: 0 if successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
